[project]
name = "lottobang"
version = "0.1.0"
description = "Heuristic lottery ticket generator for local analysis and manual purchase export."
requires-python = ">=3.12"

[tool.unittest]
start-directory = "tests"
